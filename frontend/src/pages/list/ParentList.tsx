import React from "react";
import styled from "styled-components";
const Container = styled.div`
  height: 100vh;
`;
const ParentList = () => {
  return <Container>PRList</Container>;
};

export default ParentList;
